I'm using the CEGUI-MK2 build at present.

Get it from the cegui site, build the libs, and update the include and lib paths of your project to point to where they live.

Put the .dll's in the game folder.

Need to provide a CEGui build as part of the vclibs dependencies package